{
  "permissions": {
    "//": "Kept only as a record of an earlier policy. It is held to the same restrictions as settings.json on purpose: a backup is one `cp` away from being the live policy, so it must never be the permissive copy someone restores by accident. If you no longer need the record, delete this file rather than loosening it.",
    "defaultMode": "default",
    "allow": [
      "Bash(npm run test:*)",
      "Bash(node tests/t-*.mjs)",
      "Bash(podman ps)",
      "Bash(podman ps -a)",
      "Bash(podman logs *)"
    ]
  }
}
