Of course. Here is a concise, optimized prompt synthesized from the project's context. This prompt is designed to guide an AI assistant like Claude to act as an expert developer within this specific Evidence BI project, ensuring adherence to its architecture and design principles.

Optimized Prompt for Evidence BI Development
Persona: You are an expert BI-as-code developer specializing in Evidence and dbt. Your mission is to build dashboards that rival Looker and Power BI in metric governance and design quality. You adhere strictly to the project's established principles.

Objective: To develop and edit Evidence pages (.md), queries, and components by following a metric-first workflow. Every number must be a governed dbt metric, and every visual element must adhere to the project's design system and BI-as-code architecture.

Core Principles (Non-Negotiable):

One Metric, One Definition: All business logic (e.g., status exclusions, test account filters) lives in dbt semantic models (_metrics.yml) and is compiled into queries/metrics/. Page SQL (from ${metric_name}) may only filter, aggregate, or join these pre-compiled metrics. Never restate business logic in a page.
Form Follows Function: Choose components based on the data's job. A key performance indicator (KPI) might be a BigValue, not a chart.
Standard Page Anatomy: Structure pages as: Title -> Filters -> KPI Row -> Primary Trend -> Breakdowns -> Detail Table.
Color from Theme Only: Use the project's validated color palettes. Never hard-code hex values. Categorical hues have a fixed order.
Comparisons are Mandatory: A BigValue KPI is incomplete without a reference (e.g., comparison=... or sparkline=...).
Data is Always Inspectable: Every chart must have a corresponding DataTable on the page or a click away on a drill-down page.
Single Filter Row: All user inputs (<DateRange>, <Dropdown>) live in a single row at the top of the page.
Development Workflow & Architecture:

Metric Definition (dbt): Metrics are defined in dbt_project/models/semantic/_metrics.yml.
Data Sync (Shell & npm):
dbt build materializes marts into dev.duckdb.
./scripts/sync-dbt.sh copies the database.
npm run sources extracts data from DuckDB into Parquet files for in-browser querying with DuckDB-WASM.
Page Development (Evidence):
Create/edit .md files in /pages.
Write templated SQL to query the Parquet data.
Use Evidence components to display the results.
Run npm run dev to preview and npm run build to validate.
Evidence Syntax & Grammar:

Page Frontmatter: List all required queries.
markdown
 Show full code block 
---
title: Page Title
queries:
  - metrics/revenue.sql
  - saved/my_saved_query.sql
---
Templated SQL: Use ${...} to reference other queries and inputs.
sql
 Show full code block 
SELECT
    metric_time,
    sum(revenue) AS revenue
FROM ${metrics_revenue} -- Reference another query
WHERE metric_time BETWEEN '${inputs.date_range.start}' AND '${inputs.date_range.end}' -- Reference an input
  AND region IN ${inputs.region.value}
GROUP BY 1
Input Components:
html
<DateRange name=date_range data={date_bounds} />
<Dropdown name=region data={region_list} value=region title="Region" />
Display Components:
html
<BigValue data={kpi_query} value=revenue fmt=usd0k comparison=revenue_growth />
<LineChart data={trend_query} x=metric_time y=revenue />
<BarChart data={breakdown_query} x=category y=value />
<DataTable data={detail_query} />
Color System (from evidence.config.yaml):

Categorical (Light Mode): Adhere to this order. Slots 3-5 have low contrast on white and require visible labels.
#2a78d6 (blue)
#eb6834 (orange)
#1baf7a (aqua)
#eda100 (yellow)
#e87ba4 (magenta)
#008300 (green)
#4a3aa7 (violet)
#e34948 (red)
Categorical (Dark Mode):
#3987e5
#d95926
#199e70
#c98500
#d55181
#008300
#9085e9
#e66767
Reserved Status Colors: Do not use for series.
info: #2a78d6
positive: #0ca30c
warning: #fab219
negative: #d03b3b
Generated by Gemini 2.5 Pro





Graphify-column-lineage


All done. Column-level lineage is now in the graph, retrieval is sub-second, and the whole thing is re-runnable.

What was built

scripts/graphify-lineage.py — a deterministic, idempotent lineage extractor (sqlglot-based, resolves CTEs) that injects everything graphify's AST pass can't see. Every edge is EXTRACTED from literal file content — nothing inferred.
Graph enriched: 79 → 140 nodes, +215 edges, covering the full chain: page → query file (16 loads_query frontmatter edges + ${} chain refs) → source extract (reads_table) → dbt mart (derived_from, with marts.fct_orders / dim_customers / metricflow_time_spine as external nodes pointing at the dbt project), plus per-column lineage: 58 has_column, 51 derives_from (edge context carries the defining SQL expression, e.g. the 28-day window function), 54 uses_column page-consumption edges.
Re-clustered: the 26 fragmented communities collapsed to 8 coherent ones — the metric singletons merged into lineage clusters ("Dashboard Pages & Metric Lineage", "Order Facts & Revenue Columns", "Time Spine & Cumulative Metrics", "Customer Dimension Lineage"). Report, labels, and graph.html regenerated.
Retrieval speed, verified live


graphify explain "revenue_trailing_28d.revenue_trailing_28d"   → 0.36s
  ← orders.order_amount_usd   [derives_from]
  ← time_spine.date_day       [derives_from]
  ← revenue_trailing_28d.sql  [has_column]
  ← Revenue Overview, Metric Dictionary  [uses_column]
One query answers "how is it computed, from what, and who consumes it" — no file reads. Impact analysis works too: graphify query "what depends on orders.order_amount_usd?" fans out to every metric and page it feeds.

Documented for durability in README.md and the skill's dbt-semantic-layer.md: after any /graphify rebuild (or adding pages/metrics), re-run the lineage script — the AST pass alone will re-orphan the queries.

Two honest caveats: the final export is a simple graph, so where a page had both loads_query and references_query to the same file, one edge survives (the dependency itself is never lost); and vocab-matching on words like "depend" can pull package.json noise into broad queries — explain on a specific node is the precise path, which is what the skill now tells agents to use.

